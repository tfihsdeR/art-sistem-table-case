import PostDetailsElement from '../components/post-details-element';

const PostDetails = () => {
    return (
        <section className="container">
            <PostDetailsElement />
        </section>
    );
};

export default PostDetails;
